# Node.js
# Build a general Node.js project with npm.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- azureDevops

jobs:
- job: Linux
  pool:
    vmImage: 'ubuntu-16.04'
  steps:
  - task: NodeTool@0
    inputs:
      versionSpec: '8.x'
    displayName: 'Install Node.js'
  - script:
     echo hello from Linux
     yarn install
     yarn run build
     yarn run rebuild
     yarn run test-peruse
     export {no_proxy,NO_PROXY}="127.0.0.1,localhost"
     sleep 15
     yarn run package-only
  displayName: 'yarn install,build,rebuild,test-peruse,package-only'

- job: macOS
  pool:
    vmImage: 'macOS-10.13'
  steps:
  - script:
     echo hello from Linux
     yarn install
     yarn run build
     yarn run rebuild
     yarn run test-peruse
     export {no_proxy,NO_PROXY}="127.0.0.1,localhost"
     sleep 15
     yarn run package-only
  displayName: 'yarn install,build,rebuild,test-peruse,package-only'

- job: Windows
  pool:
    vmImage: 'vs2017-win2016'
  steps:
  - script:
     echo hello from Linux
     yarn install
     yarn run build
     yarn run rebuild
     yarn run test-peruse
     export {no_proxy,NO_PROXY}="127.0.0.1,localhost"
     sleep 15
     yarn run package-only
  displayName: 'yarn install,build,rebuild,test-peruse,package-only'
